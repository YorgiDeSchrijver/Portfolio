version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm i --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
  # cache:
  #   paths:
  #     - .npm/**/*
  #     - node_modules/**/*
